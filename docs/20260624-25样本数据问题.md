## 1.hidden state都是一样的<br>
已解决，目前检查到因为数据太大，我保存的csv里的hidden state是被切片的，因为隐状态都是比较细微的差异，被切的部分刚好是一样的，现在在检查.pt文件，完整保存的hidden state是否是一样的，检查完了，不一样<br>
检查脚本打印：<br>
<img width="865" height="211" alt="image" src="https://github.com/user-attachments/assets/8387de38-29ff-4781-b2c0-be1527a719d4" />


## 2.squence不一样，为什么只有一条回答，而且打印台只显示一条回答<br>
已解决，原因：代码中传入了4.5步骤是整个数据集，数据集里只有一个问题就只会回答一次，从而将5条数据的rho覆盖<br>

## 3.大模型客观打分只需要再初始种群中ppo训练的最后一步去打分，和进化迭代中的最后一步<br>
已解决，调整了顺序<br>

## 4.变异体和初始体一样的sequnence<br>
改完第二条，这里就解决了<br>

## 5.rho重复的太多<br>
已解决，之前的提示词太严格了，现在的提示让ai自己去把握，修改如下：<br>
system_prompt = """你现在是一个SUMO路网代码校验专家，负责给回答打分，0到1分代表这个回答对于题目的完成度，不要太苛刻，该给的分要给，比如说0.1，0.2，0.5，可以有重复的分值但是不能完全一样，少数重复就行，标准由你自己去度量 """<br>
//         你是SUMO路网代码校验专家，负责评估大模型生成的路网XML代码是否准确满足了用户的需求。请严格遵守以下规则：<br>
//1. 自动过滤无关文本：忽略候选回答中的自然语言解释、思考过程、Markdown代码围栏（如```xml）等，只提取提取其中的XML代码部分进行评判。即使回答包含大量废话，只要有有效的XML，就绝不能因此打0分。<br>
//2. 动态对比需求：必须将提取出的XML代码与用户原始的需求（Prompt/Task）进行严格对比。<br>
//3. 纯净输出格式：输出必须是合法的、纯净的JSON对象，严禁输出任何Markdown标记或其他字符。格式严格为：{"score": 0.0, "reason": "理由"}。<br>
//【通用评分标准（0.0 - 1.0）】<br>
//- 0.0分：完全没有XML代码，或提取的内容根本不是合法的XML格式。<br>
//- 0.2分：具备合法的XML声明和 `<net>` 根节点，但没有任何实质性的节点(junction)或道路(edge)定义。<br>
//- 0.4分：包含了基本的节点和道路定义，但网络拓扑结构（如直路、十字、环岛、T型等）与用户设定的需求完全不符。<br>
//- 0.6分：拓扑结构基本符合需求，但在关键物理参数上有明显遗漏或错误（如：长度、限速、车道数、车道宽度、坐标位置出错）。<br>
//- 0.8分：拓扑结构和核心物理参数均符合用户需求，但在SUMO特有的细节属性上有微小瑕疵（如 priority 设置不当、内部连接关系 connection 缺失或不准确）。<br>
//- 1.0分：完美匹配用户的全部需求，参数精准，XML语法完全正确，代码逻辑严密，可直接被SUMO加载运行无报错。<br>

## 数据展示<br>
<img width="1086" height="182" alt="数据" src="https://github.com/user-attachments/assets/e65ea573-e276-4ecd-b3e0-c48c096930b0" />


## 分析数据变化<br>

### hidden state
<img width="2090" height="880" alt="hidden_state_transition_heatmap" src="https://github.com/user-attachments/assets/9f61d8eb-7da9-4580-8033-dcf6dc7ec190" />

横轴是展平后的hidden特征位置，纵轴是5组样本配对,也就是初始种群和第一次迭代的样本配对，看图整体来说接近中性，说明hidden state有变化，但不是很大，而是一些局部位置发生了细小改动。


### prm
<img width="2090" height="880" alt="prm_transition_heatmap" src="https://github.com/user-attachments/assets/b783d589-bc6f-425e-a4fc-3415c065afdb" />
<br>
图里红色多、蓝色少，说明很多位置的奖励是上升的，少数位置下降
<br>


### rho
<img width="2200" height="880" alt="rho_transition_bar" src="https://github.com/user-attachments/assets/968c9ff5-1984-4628-8db5-73dff5ebbb5e" />
<br>
第一次迭代后每个样本的 AI 评分都提升了约 0.1，有在变好

